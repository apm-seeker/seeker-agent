version: '3.8'

services:
  mysql-test2:
    image: mysql:8.0
    container_name: seeker-test2-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: seeker_test2
    volumes:
      - mysql_test2_data:/var/lib/mysql

volumes:
  mysql_test2_data:
